export default class RemovalSet<T> {
    private toDelete;
    private toDeleteSize;
    private _set;
    add(t: T): void;
    forEach(f: (t: T) => void): void;
    delete(t: T): void;
    flush(): void;
}
